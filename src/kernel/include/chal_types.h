#pragma once

typedef unsigned int  prot_domain_tag_t; /* additional architecture protection domain context (e.g. PKRU, ASID) */
typedef unsigned long pgtbl_t;	          /* a reference within the namespace of page-tables (i.e. that can be loaded into the CPU) */
