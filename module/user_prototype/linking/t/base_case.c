#include <cos_component.h>
#include <sched/cos_scheduler.h>
#define COS_FMT_PRINT
#include <print.h>

int parent_sched_get_cevt(spdid_t spdid, struct sched_child_evt *e, int idle)
{
	assert(0);
	return 0;
}

int parent_sched_child_cntl_thd(spdid_t spdid)
{
	assert(0);
	return 0;
}

int parent_sched_child_thd_crt(spdid_t spdid)
{
	assert(0);
	return 0;
}
